const MsgType = {
  RESPONSE      		    : 0,
  HARDWARE      		    : 20,
  GET_TOKEN			        : 5,
  LOAD_PROFILE_GZIPPED  : 24,
  SYNC				          : 25,
}

const exportFunctions = {
  MsgType,
};

module.exports = exportFunctions;